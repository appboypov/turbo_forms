name: turbo_forms
description: Form configuration and validation system for Flutter.
version: 1.0.0
resolution: workspace
homepage: https://github.com/appboypov/turbo_forms
repository: https://github.com/appboypov/turbo_forms

environment:
  sdk: ^3.9.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  turbo_widgets: ^1.0.0
  turbo_notifiers: ^1.0.0
  turbolytics: ^1.0.0
  equatable: ^2.0.7
  shadcn_ui: ^0.31.8
  email_validator: ^3.0.0
  gap: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
